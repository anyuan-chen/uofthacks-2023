import { Themes } from "./journey/layout";
import { msSans } from "./layout";

function getButtonFont(theme : Themes){
    if (theme === Themes.winXP){
        return msSans.className
    } else if (theme === Themes.win7) {

    } else if (theme === Themes.vercel) {

    }
}

function getParagraphFront (theme : Themes){
    if (theme === Themes.winXP){

    } else if (theme === Themes.win7) {

    } else if (theme === Themes.vercel) {

    }
}

function getHeadingFont(theme : Themes) {
    if (theme === Themes.winXP){
        
    } else if (theme === Themes.win7) {

    } else if (theme === Themes.vercel) {

    }
}